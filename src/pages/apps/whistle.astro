---
export const prerender = true;

import Base from '../../layouts/Base.astro';
import { apps, getApp } from '../../data/apps';
import VoteWidget from '../../components/VoteWidget.astro';

const app = getApp('whistle')!;
const others = apps.filter((a) => a.slug !== app.slug);

const recipients = [
  {
    n: '01',
    title: 'Emergency contacts',
    body: 'An SMS and WhatsApp message with your live location goes to everyone you have saved. They can see you on a map and message back.',
    detail: 'Up to 5 contacts',
    tone: 'cream' as const,
  },
  {
    n: '02',
    title: 'Local police',
    body: 'The closest police station to where you are gets an alert with directions to you, who is on duty, and a number to call you back.',
    detail: 'Found by your GPS',
    tone: 'cream' as const,
  },
  {
    n: '03',
    title: 'Nearest Singa Pen patrol',
    body: 'The closest Singa Pen vehicle gets the alert on its app, with directions to you. The team confirms they are on the way within seconds.',
    detail: 'Usually there in <4 min',
    tone: 'siren' as const,
  },
];

const anomalies = [
  {
    code: 'A.01',
    label: 'Vehicle idle > 25 min',
    body: 'The patrol has been parked longer than a normal break during patrol hours.',
    chip: 'Flag',
    tone: 'amber',
  },
  {
    code: 'A.02',
    label: 'Off-route',
    body: 'The vehicle has strayed more than 1.5 km outside its assigned patrol area.',
    chip: 'Investigate',
    tone: 'rust',
  },
  {
    code: 'A.03',
    label: 'Responding to alert',
    body: 'The vehicle is on its way to a Whistle alarm. Shown live until it is handled.',
    chip: 'Live',
    tone: 'leaf',
  },
  {
    code: 'A.04',
    label: 'Officer signal lost',
    body: "The patrol's signal dropped while the vehicle was still moving. The officer may be on foot.",
    chip: 'Verify',
    tone: 'amber',
  },
];

const verificationSteps = [
  {
    n: '01',
    title: 'Live face video',
    body: 'A short video of you turning your head, so we know a real person is in front of the camera right then.',
  },
  {
    n: '02',
    title: 'Photo ID',
    body: 'Take a picture of any one: Aadhaar, PAN, the TN Citizenship Card (when issued), or a current school or college ID.',
  },
  {
    n: '03',
    title: 'AI reads + deletes',
    body: 'An AI on your phone reads your ID, checks it against your face video, then deletes the image the moment it matches - it never reaches our servers. All we keep is a scrambled code that can\'t be turned back into your ID, just to stop the same ID being used twice.',
  },
];

const ldJson = {
  '@context': 'https://schema.org',
  '@type': 'SoftwareApplication',
  name: app.name,
  alternateName: app.tamilName,
  description: app.shortDescription,
  applicationCategory: app.category,
  operatingSystem: 'Android, iOS, Web',
  inLanguage: ['ta', 'en'],
};

const faqJson = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: app.faq.map((f) => ({
    '@type': 'Question',
    name: f.q,
    acceptedAnswer: { '@type': 'Answer', text: f.a },
  })),
};
---
<Base
  title={app.meta.title}
  description={app.meta.description}
  keywords={app.meta.keywords}
  type="article"
>
  <!-- HERO -->
  <section class="relative isolate overflow-hidden bg-[var(--color-ink)] text-[var(--color-cream)] grain">
    <div aria-hidden="true" class="absolute -top-40 -right-40 h-[700px] w-[700px] rounded-full blur-3xl opacity-25"
      style="background: radial-gradient(closest-side, var(--color-siren), transparent 70%);"></div>
    <div aria-hidden="true" class="absolute -bottom-40 -left-32 h-[460px] w-[460px] rounded-full blur-3xl opacity-15"
      style="background: radial-gradient(closest-side, var(--color-rust), transparent 70%);"></div>

    <div class="container-x relative pt-20 pb-24 md:pt-28 md:pb-32">
      <nav class="text-xs text-[var(--color-cream)]/55 mb-12">
        <a href="/" class="hover:text-[var(--color-cream)]">tvkapps</a>
        <span class="mx-2 opacity-50">/</span>
        <a href="/#suite" class="hover:text-[var(--color-cream)]">apps</a>
        <span class="mx-2 opacity-50">/</span>
        <span class="text-[var(--color-cream)]/85">Whistle</span>
      </nav>

      <div class="grid grid-cols-12 gap-6 lg:gap-8 items-end">
        <div class="col-span-12 lg:col-span-8">
          <span class="eyebrow" style="color: color-mix(in srgb, var(--color-siren) 80%, white);">{app.category} &nbsp;/&nbsp; App 02</span>
          <div class="mt-4 flex items-end gap-3 sm:gap-5 flex-wrap">
            <h1 class="font-display text-[48px] sm:text-[80px] md:text-[112px] lg:text-[128px] leading-[0.92] tracking-[-0.025em]">
              Whistle.
            </h1>
            <span class="font-tamil text-xl sm:text-3xl md:text-4xl text-[var(--color-cream)]/55 pb-2 sm:pb-4">{app.tamilName}</span>
          </div>
          <p class="mt-6 font-display text-xl sm:text-2xl md:text-4xl italic font-[450] max-w-2xl"
            style="color: color-mix(in srgb, var(--color-siren) 70%, white);">
            {app.tagline}
          </p>
          <p class="mt-3 text-[12px] sm:text-[13px] font-mono uppercase tracking-[0.2em] text-[var(--color-rust)]">
            &mdash;&nbsp; If you have a problem, just whistle.
          </p>
        </div>
        <div class="col-span-12 lg:col-span-4 flex flex-wrap gap-3 lg:justify-end items-center">
          <a href="#download" class="btn bg-[var(--color-siren)] text-[var(--color-cream)] hover:bg-[var(--color-siren-deep)]">
            Get the app
          </a>
          <a href="/" class="btn border border-[var(--color-cream)]/25 text-[var(--color-cream)] hover:bg-[var(--color-cream)]/10">
            All apps
          </a>
          <VoteWidget slug={app.slug} accent="siren" variant="dark" />
        </div>
      </div>

      <p class="mt-10 md:mt-12 max-w-3xl text-[16px] sm:text-[18px] md:text-[20px] leading-[1.55] text-[var(--color-cream)]/75">
        {app.longDescription}
      </p>

      <!-- stats strip -->
      <div class="mt-16 md:mt-20 grid grid-cols-3 border-t border-[var(--color-cream)]/15">
        {app.stats.map((s) => (
          <div class="border-r last:border-r-0 border-[var(--color-cream)]/15 py-8 md:py-12 px-4 sm:px-6 first:pl-0">
            <p class="font-display text-2xl sm:text-3xl md:text-5xl lg:text-6xl tracking-[-0.02em] leading-none"
              style="color: color-mix(in srgb, var(--color-siren) 75%, white);">{s.value}</p>
            <p class="text-[10px] sm:text-xs uppercase tracking-wider text-[var(--color-cream)]/55 mt-3 md:mt-5 leading-tight">{s.label}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- THE SOS BUTTON + RECIPIENTS -->
  <section class="bg-[var(--color-cream)] py-24 md:py-32 relative overflow-hidden">
    <div aria-hidden="true" class="absolute -top-32 -right-20 h-[460px] w-[460px] rounded-full blur-3xl opacity-15"
      style="background: radial-gradient(closest-side, var(--color-rust), transparent 70%);"></div>

    <div class="container-x relative">
      <div class="grid grid-cols-12 gap-y-10 md:gap-10 items-end mb-14 md:mb-16">
        <div class="col-span-12 md:col-span-7">
          <span class="eyebrow text-[var(--color-rust)]">The button</span>
          <h2 class="mt-3 font-display text-[36px] sm:text-[56px] md:text-[64px] leading-[1.0] tracking-[-0.02em] max-w-3xl">
            One tap.<br/>
            <span class="italic font-[450]">Three responders.</span>
          </h2>
        </div>
        <div class="col-span-12 md:col-span-5">
          <p class="text-[15px] text-[var(--color-ink)]/70 max-w-md leading-relaxed">
            A shortcut on your home screen. Press it in a panic, or after a long uneasy walk. The moment you lift your thumb, three
            alerts go out at once - and the phone starts whistling at full volume.
          </p>
        </div>
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-[1fr_1.2fr] gap-8 items-stretch">
        <!-- SOS button rendering -->
        <article class="rounded-[28px] bg-[var(--color-ink)] text-[var(--color-cream)] p-8 md:p-12 relative overflow-hidden grid place-items-center min-h-[420px]">
          <div aria-hidden="true" class="absolute inset-0"
            style="background: radial-gradient(circle at 50% 55%, color-mix(in srgb, var(--color-rust) 18%, transparent), transparent 65%);"></div>

          <div class="relative flex flex-col items-center text-center">
            <div class="relative grid place-items-center">
              <span aria-hidden="true" class="absolute h-[280px] w-[280px] rounded-full border border-[var(--color-rust)] opacity-15 animate-ping" style="animation-duration: 2.6s;"></span>
              <span aria-hidden="true" class="absolute h-[230px] w-[230px] rounded-full border border-[var(--color-rust)] opacity-25"></span>
              <span aria-hidden="true" class="absolute h-[190px] w-[190px] rounded-full border-2 border-[var(--color-rust)] opacity-40"></span>
              <button class="relative h-[160px] w-[160px] rounded-full text-[var(--color-cream)] shadow-[0_30px_80px_-20px_rgba(214,41,62,0.6)]"
                style="background: radial-gradient(circle at 35% 30%, color-mix(in srgb, var(--color-rust) 80%, white), var(--color-rust) 55%, var(--color-rust-deep));">
                <svg class="mx-auto" width="52" height="52" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
                  <rect x="3" y="10.5" width="4" height="3" rx="0.5"/>
                  <path d="M7 9 H14 A4 4 0 0 1 14 17 H7 Z"/>
                  <circle cx="11.5" cy="13" r="1.4" fill="var(--color-rust-deep)"/>
                  <circle cx="17.5" cy="7.5" r="1.3" fill="none" stroke="currentColor" stroke-width="1.4"/>
                </svg>
                <span class="block text-[12px] font-bold uppercase tracking-[0.2em] mt-2">SOS</span>
              </button>
            </div>
            <p class="mt-10 text-[13px] uppercase tracking-[0.18em] text-[var(--color-cream)]/65">Hold for one second</p>
            <p class="mt-2 font-display text-xl italic text-[var(--color-rust)]">"If you have a problem, just whistle."</p>
          </div>
        </article>

        <!-- 3 recipients vertical -->
        <ul class="space-y-3">
          {recipients.map((r) => (
            <li>
              <article class={`rounded-2xl border border-[var(--color-line)] p-5 md:p-6 ${r.tone === 'siren' ? 'bg-[color-mix(in_srgb,var(--color-siren)_8%,var(--color-cream))]' : 'bg-[var(--color-cream-2)]'}`}>
                <div class="flex items-baseline justify-between gap-3 mb-3 flex-wrap">
                  <span class="font-mono text-[11px]" style={`color: ${r.tone === 'siren' ? 'var(--color-siren-deep)' : 'var(--color-rust)'};`}>R.{r.n}</span>
                  <span class="text-[10.5px] uppercase tracking-wider text-[var(--color-ink)]/55 font-mono">{r.detail}</span>
                </div>
                <h3 class="font-display text-2xl tracking-tight">{r.title}</h3>
                <p class="mt-2 text-[14px] text-[var(--color-ink)]/70 leading-relaxed">{r.body}</p>
              </article>
            </li>
          ))}
        </ul>
      </div>

      <!-- audible whistle callout -->
      <div class="mt-12 rounded-2xl bg-[var(--color-cream-2)] border border-[var(--color-line)] p-6 md:p-8 grid grid-cols-1 md:grid-cols-[auto_1fr] gap-5 md:gap-8 items-center">
        <div class="grid place-items-center h-14 w-14 rounded-full bg-[var(--color-rust)] text-[var(--color-cream)] shrink-0">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
            <rect x="3" y="10.5" width="4" height="3" rx="0.5"/>
            <path d="M7 9 H14 A4 4 0 0 1 14 17 H7 Z"/>
            <circle cx="11.5" cy="13" r="1.4" fill="var(--color-rust)"/>
            <circle cx="17.5" cy="7.5" r="1.3" fill="none" stroke="currentColor" stroke-width="1.4"/>
          </svg>
        </div>
        <div>
          <h3 class="font-display text-2xl tracking-tight">The phone whistles too.</h3>
          <p class="mt-2 text-[14px] text-[var(--color-ink)]/70 leading-relaxed max-w-3xl">
            When you press it, the phone plays a loud whistle sound at full volume - to scare off whoever is bothering you,
            and to make people nearby look over. It only stops when you cancel it with your PIN, or when a Singa Pen officer marks you safe.
            You can turn it off, but it is on to start with.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- SINGA PEN PATROL -->
  <section class="relative bg-[var(--color-cream-2)] py-24 md:py-32">
    <div class="container-x">
      <div class="grid grid-cols-12 gap-y-10 md:gap-10 items-end mb-14 md:mb-16">
        <div class="col-span-12 md:col-span-7">
          <span class="eyebrow text-[var(--color-siren-deep)]">The patrol</span>
          <h2 class="mt-3 font-display text-[36px] sm:text-[56px] md:text-[64px] leading-[1.0] tracking-[-0.02em] max-w-3xl">
            Singa Pen.<br/>
            <span class="italic font-[450]" style="color: var(--color-siren-deep);">A lion of a force.</span>
          </h2>
          <p class="mt-4 font-tamil text-2xl text-[var(--color-ink)]/55">சிங்கப்பெண்</p>
        </div>
        <div class="col-span-12 md:col-span-5">
          <p class="text-[15px] text-[var(--color-ink)]/70 max-w-md leading-relaxed">
            A women's patrol force set up by the Tamil Nadu government. Every patrol vehicle has the
            Whistle patrol app. When a Whistle alarm goes off, the closest vehicle gets the alert and step-by-step directions to you.
            Across the state, they usually reach you in under four minutes.
          </p>
        </div>
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 items-start">
        <!-- patrol app mockup -->
        <article class="rounded-[28px] bg-[var(--color-cream)] border border-[var(--color-line)] p-6 md:p-8">
          <div class="flex items-center justify-between gap-3 mb-5 flex-wrap">
            <span class="rounded-full bg-[var(--color-siren)] text-[var(--color-cream)] px-3 py-1.5 text-[11px] font-semibold uppercase tracking-wider">Patrol app</span>
            <span class="font-mono text-[11px] text-[var(--color-ink)]/45">Singa Pen vehicle #SP-238</span>
          </div>

          <!-- incoming alert card -->
          <div class="rounded-2xl bg-[var(--color-ink)] text-[var(--color-cream)] p-5 relative overflow-hidden">
            <div aria-hidden="true" class="absolute -top-12 -right-12 h-32 w-32 rounded-full blur-2xl opacity-40"
              style="background: radial-gradient(closest-side, var(--color-rust), transparent 70%);"></div>
            <div class="relative">
              <div class="flex items-center justify-between">
                <div class="flex items-center gap-2">
                  <span class="relative flex">
                    <span class="absolute h-2.5 w-2.5 rounded-full bg-[var(--color-rust)] opacity-60 animate-ping"></span>
                    <span class="relative h-2.5 w-2.5 rounded-full bg-[var(--color-rust)]"></span>
                  </span>
                  <span class="text-[11px] uppercase tracking-wider text-[var(--color-rust)] font-bold">Incoming alert</span>
                </div>
                <span class="font-mono text-[10.5px] text-[var(--color-cream)]/55">09:42:18</span>
              </div>
              <p class="mt-3 font-display text-xl">Whistle &middot; W-04 821</p>
              <p class="text-[12.5px] text-[var(--color-cream)]/65 mt-1">Anna Nagar · 4th Avenue near AP-12</p>
              <div class="mt-4 grid grid-cols-3 gap-2 text-center">
                <div class="rounded-xl bg-[var(--color-cream)]/8 p-2">
                  <p class="text-[10px] uppercase tracking-wider text-[var(--color-cream)]/55">Distance</p>
                  <p class="font-display text-base mt-0.5">1.2 km</p>
                </div>
                <div class="rounded-xl bg-[var(--color-cream)]/8 p-2">
                  <p class="text-[10px] uppercase tracking-wider text-[var(--color-cream)]/55">ETA</p>
                  <p class="font-display text-base mt-0.5">3 m 40 s</p>
                </div>
                <div class="rounded-xl bg-[var(--color-cream)]/8 p-2">
                  <p class="text-[10px] uppercase tracking-wider text-[var(--color-cream)]/55">ID</p>
                  <p class="font-display text-base mt-0.5">Verified</p>
                </div>
              </div>
              <button class="mt-4 w-full rounded-full bg-[var(--color-rust)] text-[var(--color-cream)] py-3 text-[13px] font-semibold">
                Accept &amp; start driving
              </button>
            </div>
          </div>

          <!-- patrol log -->
          <div class="mt-5 rounded-xl bg-[var(--color-cream-2)] p-4">
            <p class="text-[10px] uppercase tracking-wider text-[var(--color-ink)]/50 mb-2">Today</p>
            <ul class="space-y-2 text-[12.5px]">
              <li class="flex items-center justify-between">
                <span>3 alerts responded</span>
                <span class="font-mono text-[var(--color-leaf-deep)]">all cleared</span>
              </li>
              <li class="flex items-center justify-between">
                <span>Median response</span>
                <span class="font-mono">3 m 11 s</span>
              </li>
              <li class="flex items-center justify-between">
                <span>Patrol km</span>
                <span class="font-mono">38.4</span>
              </li>
            </ul>
          </div>
        </article>

        <!-- patrol map mockup -->
        <article class="rounded-[28px] bg-[var(--color-ink)] text-[var(--color-cream)] p-6 md:p-8 relative overflow-hidden">
          <div aria-hidden="true" class="absolute inset-0 opacity-40"
            style="background: radial-gradient(circle at 70% 30%, color-mix(in srgb, var(--color-siren) 30%, transparent), transparent 60%);"></div>

          <div class="relative">
            <div class="flex items-center justify-between gap-3 mb-5 flex-wrap">
              <span class="rounded-full bg-[var(--color-cream)]/10 border border-[var(--color-cream)]/20 px-3 py-1.5 text-[11px] font-semibold uppercase tracking-wider">Live coverage</span>
              <span class="font-mono text-[11px] text-[var(--color-cream)]/55">Anna Nagar &middot; TN-03</span>
            </div>

            <!-- abstract grid map with vehicle dots -->
            <div class="relative rounded-2xl border border-[var(--color-cream)]/15 overflow-hidden h-[280px] bg-[color-mix(in_srgb,var(--color-ink)_85%,var(--color-siren))]">
              <svg viewBox="0 0 400 280" class="absolute inset-0 w-full h-full">
                <defs>
                  <pattern id="grid" width="20" height="20" patternUnits="userSpaceOnUse">
                    <path d="M 20 0 L 0 0 0 20" fill="none" stroke="rgba(250,247,242,0.07)" stroke-width="0.5"/>
                  </pattern>
                </defs>
                <rect width="400" height="280" fill="url(#grid)"/>
                <!-- roads -->
                <path d="M 0 110 L 400 130" stroke="rgba(250,247,242,0.12)" stroke-width="1.2" fill="none"/>
                <path d="M 0 200 L 400 195" stroke="rgba(250,247,242,0.12)" stroke-width="1.2" fill="none"/>
                <path d="M 120 0 L 130 280" stroke="rgba(250,247,242,0.12)" stroke-width="1.2" fill="none"/>
                <path d="M 280 0 L 270 280" stroke="rgba(250,247,242,0.12)" stroke-width="1.2" fill="none"/>
                <!-- patrol vehicle dots (siren blue) -->
                <g>
                  <circle cx="80" cy="160" r="6" fill="var(--color-siren)"/>
                  <circle cx="80" cy="160" r="11" fill="none" stroke="var(--color-siren)" stroke-width="1" opacity="0.4"/>
                </g>
                <g>
                  <circle cx="220" cy="80" r="6" fill="var(--color-siren)"/>
                  <circle cx="220" cy="80" r="11" fill="none" stroke="var(--color-siren)" stroke-width="1" opacity="0.4"/>
                </g>
                <g>
                  <circle cx="320" cy="220" r="6" fill="var(--color-siren)"/>
                  <circle cx="320" cy="220" r="11" fill="none" stroke="var(--color-siren)" stroke-width="1" opacity="0.4"/>
                </g>
                <!-- active alert (rust) -->
                <g>
                  <circle cx="190" cy="170" r="8" fill="var(--color-rust)">
                    <animate attributeName="r" from="6" to="12" dur="1.4s" repeatCount="indefinite"/>
                    <animate attributeName="opacity" from="0.9" to="0.4" dur="1.4s" repeatCount="indefinite"/>
                  </circle>
                  <circle cx="190" cy="170" r="6" fill="var(--color-rust)"/>
                </g>
                <!-- route line -->
                <path d="M 220 80 Q 200 130 190 170" fill="none" stroke="var(--color-rust)" stroke-width="2" stroke-dasharray="4 4">
                  <animate attributeName="stroke-dashoffset" from="0" to="-16" dur="0.8s" repeatCount="indefinite"/>
                </path>
              </svg>
            </div>

            <div class="mt-4 flex items-center justify-between text-[12px] flex-wrap gap-3">
              <div class="flex items-center gap-4">
                <span class="flex items-center gap-1.5"><span class="h-2 w-2 rounded-full" style="background: var(--color-siren);"></span><span>3 patrols</span></span>
                <span class="flex items-center gap-1.5"><span class="h-2 w-2 rounded-full bg-[var(--color-rust)]"></span><span>1 active alert</span></span>
              </div>
              <span class="font-mono text-[11px] text-[var(--color-cream)]/55">only the Singa Pen control room sees this, never the public</span>
            </div>
          </div>
        </article>
      </div>
    </div>
  </section>

  <!-- ADMIN DASHBOARD + ANOMALIES -->
  <section class="bg-[var(--color-ink)] text-[var(--color-cream)] py-24 md:py-32 relative overflow-hidden">
    <div aria-hidden="true" class="absolute -top-32 left-1/2 -translate-x-1/2 h-[500px] w-[900px] rounded-full blur-3xl opacity-10"
      style="background: radial-gradient(closest-side, var(--color-siren), transparent 70%);"></div>

    <div class="container-x relative">
      <div class="grid grid-cols-12 gap-y-10 md:gap-10 items-end mb-14 md:mb-16">
        <div class="col-span-12 md:col-span-7">
          <span class="eyebrow" style="color: color-mix(in srgb, var(--color-siren) 80%, white);">Keeping watch on the patrols</span>
          <h2 class="mt-3 font-display text-[36px] sm:text-[56px] md:text-[64px] leading-[1.0] tracking-[-0.02em] max-w-3xl">
            Anomalies surface.<br/>
            <span class="italic font-[450] text-[var(--color-rust)]">Action follows.</span>
          </h2>
        </div>
        <div class="col-span-12 md:col-span-5">
          <p class="text-[15px] text-[var(--color-cream)]/70 max-w-md leading-relaxed">
            The Singa Pen control room sees a live grid of every vehicle on duty - and the system watches for anything off,
            flagging any patrol that isn't following its schedule. Parked vehicles, patrols that have wandered off their area, dropped officer signals,
            patrols out on a call - all clear at a glance.
          </p>
        </div>
      </div>

      <!-- anomalies grid -->
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-px bg-[var(--color-cream)]/12 rounded-2xl overflow-hidden">
        {anomalies.map((a) => (
          <article class="bg-[var(--color-ink)] p-6 md:p-7">
            <div class="flex items-center justify-between mb-6">
              <span class="font-mono text-[11px]"
                style={`color: ${a.tone === 'rust' ? 'var(--color-rust)' : a.tone === 'leaf' ? 'var(--color-leaf)' : a.tone === 'amber' ? 'rgb(217,119,6)' : 'var(--color-cream)'};`}>
                {a.code}
              </span>
              <span class="text-[10px] uppercase tracking-wider text-[var(--color-cream)]/55 font-mono">{a.chip}</span>
            </div>
            <h3 class="font-display text-xl tracking-tight">{a.label}</h3>
            <p class="mt-2 text-[13.5px] text-[var(--color-cream)]/65 leading-relaxed">{a.body}</p>
          </article>
        ))}
      </div>

      <!-- dashboard mock row -->
      <div class="mt-12 rounded-2xl bg-[var(--color-cream)]/6 border border-[var(--color-cream)]/12 overflow-hidden">
        <div class="px-5 py-3 border-b border-[var(--color-cream)]/12 flex flex-col sm:flex-row sm:items-center sm:justify-between gap-3">
          <div>
            <p class="text-[10px] uppercase tracking-wider text-[var(--color-cream)]/50">Singa Pen oversight &middot; live</p>
            <p class="font-display text-base">Chennai zone &middot; 14:02</p>
          </div>
          <div class="flex items-center gap-3 text-[11px] font-mono flex-wrap">
            <span class="flex items-center gap-1.5"><span class="h-2 w-2 rounded-full" style="background: var(--color-siren);"></span>62 on duty</span>
            <span class="flex items-center gap-1.5"><span class="h-2 w-2 rounded-full bg-[var(--color-rust)]"></span>3 responding</span>
            <span class="flex items-center gap-1.5"><span class="h-2 w-2 rounded-full bg-amber-500"></span>2 anomalies</span>
            <span class="flex items-center gap-1.5"><span class="h-2 w-2 rounded-full bg-[var(--color-leaf)]"></span>0 unresolved alerts</span>
          </div>
        </div>
        <ul class="divide-y divide-[var(--color-cream)]/10">
          {[
            { v: 'SP-112', loc: 'T. Nagar South', status: 'Responding · W-04 821', tone: 'rust', t: '3 m ago' },
            { v: 'SP-238', loc: 'Anna Nagar', status: 'On route to alert', tone: 'rust', t: '1 m ago' },
            { v: 'SP-067', loc: 'Mylapore', status: 'Patrolling', tone: 'siren', t: 'now' },
            { v: 'SP-301', loc: 'Egmore', status: 'Idle &middot; 28 min', tone: 'amber', t: '5 m ago' },
            { v: 'SP-194', loc: 'Adyar', status: 'Off-route 1.8 km', tone: 'amber', t: '2 m ago' },
          ].map((row) => (
            <li class="px-5 py-3">
              <div class="flex items-start justify-between gap-3">
                <div class="flex items-center gap-2 min-w-0 flex-1">
                  <span class="h-2 w-2 rounded-full shrink-0"
                    style={`background: ${row.tone === 'rust' ? 'var(--color-rust)' : row.tone === 'siren' ? 'var(--color-siren)' : 'rgb(217,119,6)'};`}></span>
                  <span class="font-mono text-[11px] text-[var(--color-cream)]/55 shrink-0">{row.v}</span>
                  <span class="font-medium text-[12.5px] truncate">{row.loc}</span>
                </div>
                <span class="font-mono text-[10.5px] text-[var(--color-cream)]/45 shrink-0">{row.t}</span>
              </div>
              <p class="mt-1 ml-[18px] text-[11px] text-[var(--color-cream)]/65" set:html={row.status}></p>
            </li>
          ))}
        </ul>
      </div>

      <p class="mt-6 text-[12.5px] text-[var(--color-cream)]/55 max-w-3xl">
        Each patrol vehicle's GPS only the Singa Pen control room sees, never the public. The one number the public can see
        is the total count of patrols on duty in each constituency.
      </p>
    </div>
  </section>

  <!-- IDENTITY VERIFICATION -->
  <section class="bg-[var(--color-cream)] py-24 md:py-32">
    <div class="container-x">
      <div class="grid grid-cols-12 gap-y-10 md:gap-10 items-end mb-14 md:mb-16">
        <div class="col-span-12 md:col-span-7">
          <span class="eyebrow text-[var(--color-rust)]">The gate</span>
          <h2 class="mt-3 font-display text-[36px] sm:text-[56px] md:text-[64px] leading-[1.0] tracking-[-0.02em] max-w-3xl">
            Whistle only opens<br/>
            <span class="italic font-[450]">to verified women.</span>
          </h2>
        </div>
        <div class="col-span-12 md:col-span-5">
          <p class="text-[15px] text-[var(--color-ink)]/70 max-w-md leading-relaxed">
            Apps like this get abused the moment they launch - men making fake accounts to test response time
            or just to waste the patrol's time. So before you can press the button, you verify your ID once.
          </p>
        </div>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-px bg-[var(--color-line)] rounded-2xl overflow-hidden border border-[var(--color-line)]">
        {verificationSteps.map((s) => (
          <article class="bg-[var(--color-cream)] p-7 md:p-8">
            <span class="font-mono text-[11px] text-[var(--color-rust)]">V.{s.n}</span>
            <h3 class="mt-5 font-display text-2xl tracking-tight">{s.title}</h3>
            <p class="mt-2 text-[14px] text-[var(--color-ink)]/70 leading-relaxed">{s.body}</p>
          </article>
        ))}
      </div>

      <div class="mt-10 flex flex-wrap items-center gap-3 text-[13px] text-[var(--color-ink)]/70">
        <span class="text-[var(--color-ink)]/55">Accepted IDs:</span>
        {['Aadhaar', 'PAN', 'TN Citizenship Card', 'School / College ID'].map((id) => (
          <span class="rounded-full bg-[var(--color-cream-2)] border border-[var(--color-line)] px-3 py-1.5 font-medium">{id}</span>
        ))}
      </div>

      <!-- ID-never-stored callout: maximum-security guarantee -->
      <div class="mt-10 rounded-2xl border border-[var(--color-line)] bg-[var(--color-ink)] text-[var(--color-cream)] p-6 md:p-8 grid grid-cols-1 md:grid-cols-[auto_1fr] gap-5 md:gap-8 items-start relative overflow-hidden">
        <div aria-hidden="true" class="absolute -top-12 -right-12 h-40 w-40 rounded-full blur-3xl opacity-25"
          style="background: radial-gradient(closest-side, var(--color-siren), transparent 70%);"></div>
        <div class="relative grid place-items-center h-12 w-12 rounded-full bg-[color-mix(in_srgb,var(--color-siren)_20%,transparent)] text-[var(--color-siren)] shrink-0">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <rect x="3" y="11" width="18" height="11" rx="2"/>
            <path d="M7 11V7a5 5 0 0 1 10 0v4"/>
          </svg>
        </div>
        <div class="relative">
          <p class="font-mono text-[11px] uppercase tracking-[0.18em] text-[var(--color-siren)]">Privacy by design</p>
          <h3 class="mt-2 font-display text-xl md:text-2xl tracking-tight">Your ID never reaches our servers.</h3>
          <p class="mt-2 text-[14px] text-[var(--color-cream)]/70 leading-relaxed max-w-2xl">
            An AI on your phone reads your ID, checks it against your face video, then deletes the image the moment it matches
            - it never reaches our servers. All we keep is a scrambled code that can't be turned back into your ID,
            just to stop the same ID being used twice.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- FEATURES -->
  <section class="bg-[var(--color-cream-2)] py-24 md:py-32">
    <div class="container-x">
      <div class="mb-12">
        <span class="eyebrow text-[var(--color-rust)]">What it does</span>
        <h2 class="mt-3 font-display text-[40px] md:text-[56px] leading-[1.02] tracking-[-0.02em] max-w-2xl">
          Six features.<br/>
          <span class="italic font-[450]">Built for the worst minute of someone's day.</span>
        </h2>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-px bg-[var(--color-line)] rounded-2xl overflow-hidden border border-[var(--color-line)]">
        {app.features.map((f, i) => (
          <article class="bg-[var(--color-cream)] p-8 md:p-10">
            <div class="flex items-center gap-2 mb-6">
              <span class="font-mono text-[11px] text-[var(--color-rust)]">F.0{i + 1}</span>
              <span class="h-px flex-1 bg-[var(--color-line)]"></span>
            </div>
            <h3 class="font-display text-2xl tracking-tight">{f.title}</h3>
            <p class="mt-3 text-[15px] text-[var(--color-ink)]/70 leading-relaxed">{f.body}</p>
          </article>
        ))}
      </div>
    </div>
  </section>

  <!-- FAQ -->
  <section class="py-24 md:py-32 bg-[var(--color-cream)]">
    <div class="container-x">
      <div class="grid grid-cols-12 gap-y-10 md:gap-10">
        <div class="col-span-12 md:col-span-4">
          <span class="eyebrow text-[var(--color-rust)]">FAQ</span>
          <h2 class="mt-3 font-display text-[36px] md:text-[52px] leading-[1.02] tracking-[-0.02em]">
            Honest answers,<br/>
            <span class="italic font-[450]" style="color: var(--color-siren-deep);">up front.</span>
          </h2>
        </div>
        <div class="col-span-12 md:col-span-8">
          <div class="divide-y divide-[var(--color-line)] border-y border-[var(--color-line)]">
            {app.faq.map((item) => (
              <details class="group py-5">
                <summary class="flex cursor-pointer list-none items-start justify-between gap-6">
                  <span class="font-display text-xl tracking-tight">{item.q}</span>
                  <span class="shrink-0 rounded-full border border-[var(--color-line)] h-7 w-7 grid place-items-center transition group-open:rotate-45">
                    <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 5v14M5 12h14"/></svg>
                  </span>
                </summary>
                <p class="mt-3 max-w-2xl text-[15px] text-[var(--color-ink)]/70 leading-relaxed">{item.a}</p>
              </details>
            ))}
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- OTHER APPS -->
  <section class="bg-[var(--color-cream-2)] py-24 md:py-32">
    <div class="container-x">
      <span class="eyebrow text-[var(--color-ink)]/55">Continue exploring</span>
      <h2 class="mt-3 font-display text-[36px] md:text-[48px] leading-[1.02] tracking-[-0.02em] max-w-xl">
        The other apps in the suite.
      </h2>

      <div class="mt-12 grid grid-cols-1 md:grid-cols-3 gap-6">
        {others.map((o) => (
          <a href={`/apps/${o.slug}`} class="group rounded-[var(--radius-card)] border border-[var(--color-line)] bg-[var(--color-cream)] p-8 hover:-translate-y-0.5 hover:shadow-[0_30px_60px_-30px_rgba(14,14,14,0.18)] transition">
            <div class="flex items-center justify-between">
              <span class={`h-2 w-2 rounded-full ${o.accent === 'rust' ? 'bg-[var(--color-rust)]' : o.accent === 'leaf' ? 'bg-[var(--color-leaf)]' : o.accent === 'gold' ? 'bg-[var(--color-gold)]' : o.accent === 'siren' ? 'bg-[var(--color-siren)]' : o.accent === 'pulse' ? 'bg-[var(--color-pulse)]' : o.accent === 'quill' ? 'bg-[var(--color-quill)]' : 'bg-[var(--color-iris)]'}`}></span>
              <span class="font-tamil text-sm text-[var(--color-ink)]/50">{o.tamilName}</span>
            </div>
            <h3 class="mt-6 font-display text-3xl tracking-tight">{o.name}</h3>
            <p class="mt-2 text-[15px] text-[var(--color-ink)]/65 max-w-md">{o.tagline}</p>
            <div class="mt-6 flex items-center gap-2 text-sm font-medium">
              <span class="opacity-70 group-hover:opacity-100 transition">Read more</span>
              <svg class="transition group-hover:translate-x-0.5" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M13 5l7 7-7 7"/></svg>
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- DOWNLOAD -->
  <section id="download" class="relative py-24 md:py-32 bg-[var(--color-cream)]">
    <div class="container-x">
      <div class="rounded-[28px] p-7 sm:p-10 md:p-16 grain relative overflow-hidden"
        style="background: linear-gradient(135deg, var(--color-siren) 0%, var(--color-siren-deep) 100%); color: var(--color-cream);">
        <div aria-hidden="true" class="absolute -bottom-32 -left-20 h-[420px] w-[420px] rounded-full blur-3xl opacity-25"
          style="background: radial-gradient(closest-side, var(--color-rust), transparent 70%);"></div>
        <div class="relative grid grid-cols-12 gap-y-8 md:gap-10 items-end">
          <div class="col-span-12 md:col-span-7">
            <span class="eyebrow text-[var(--color-cream)]/80">Get Whistle</span>
            <h2 class="mt-4 font-display text-[40px] sm:text-[56px] md:text-[64px] lg:text-[72px] leading-[1.0] tracking-[-0.02em]">
              One tap.<br/>One whistle.<br/>One patrol on its way.
            </h2>
          </div>
          <div class="col-span-12 md:col-span-5">
            <form class="rounded-2xl bg-[var(--color-cream)]/12 backdrop-blur border border-[var(--color-cream)]/20 p-3 flex items-center gap-2">
              <input type="email" required placeholder="you@example.com"
                class="flex-1 bg-transparent px-3 py-2.5 text-[15px] text-[var(--color-cream)] placeholder:text-[var(--color-cream)]/55 focus:outline-none"/>
              <button type="submit" class="btn bg-[var(--color-cream)] text-[var(--color-siren-deep)] hover:bg-white">
                Notify me
              </button>
            </form>
            <p class="mt-3 text-[11px] text-[var(--color-cream)]/65">
              The first versions, with the Singa Pen control room side, go live as the force rolls out.
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <script type="application/ld+json" set:html={JSON.stringify(ldJson)} />
  <script type="application/ld+json" set:html={JSON.stringify(faqJson)} />
</Base>
